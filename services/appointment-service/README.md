# Appointment Service

Creates, updates, and tracks patient appointments.

- Default port: `5003`
- Health: `GET /health`
- API docs: `GET /swagger`

Run with Docker Compose from repo root:

```bash
docker compose -f infra/docker-compose.yml up -d appointment-service
```
