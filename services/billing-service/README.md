# Billing Service

Creates bills and records payments for patient visits.

- Default port: `5005`
- Health: `GET /health`
- API docs: `GET /swagger`

Run with Docker Compose from repo root:

```bash
docker compose -f infra/docker-compose.yml up -d billing-service
```
