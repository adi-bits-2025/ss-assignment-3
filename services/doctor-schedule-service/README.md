# Doctor Schedule Service

Manages doctor profiles and available time slots.

- Default port: `5002`
- Health: `GET /health`
- API docs: `GET /swagger`

Run with Docker Compose from repo root:

```bash
docker compose -f infra/docker-compose.yml up -d doctor-schedule-service
```
