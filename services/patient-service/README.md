# Patient Service

Handles patient registration and lookup.

- Default port: `5001`
- Health: `GET /health`
- API docs: `GET /swagger`

Run with Docker Compose from repo root:

```bash
docker compose -f infra/docker-compose.yml up -d patient-service
```
