# Prescription Service

Issues and manages prescriptions linked to appointments.

- Default port: `5004`
- Health: `GET /health`
- API docs: `GET /swagger`

Run with Docker Compose from repo root:

```bash
docker compose -f infra/docker-compose.yml up -d prescription-service
```
